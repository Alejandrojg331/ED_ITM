crear un método que me consulte un nodo de la lista y compare la notaDef 
con el nodo anterior y el nodo siguiente, con el fin de identificar 
cual de ellos tiene mejor nota

Nodo start, end
Nodo p,q

publico vacio comparacionNodos(ListaDoble ld,entero cod)
  real n1, n2
  p = ld.getStart()
  entero aux = ld.getStart().getCodigo()
  si(!buscar(cod)entonces
     escribir("estudiante no se encuentra en la lista")
   sino
     mientras(p!=null)
       entero id = (Estudiantes)p.getCodigo()
        si(id == cod)&&(p.getAnt() == nulo)entonces
           n1 = (Estudiantes)p.getSig().getNotaDef()
           n2 = (Estudiantes)p.getNotaDef()
            si(n1>n2)entonces
             escriba("el estudiante con código"+ (Estudiantes)p.getSig().getCodigo()+" tiene my nota")
             sino si (n2>n1)entonces  
               escriba("el estudiante con código "+ (Estudiantes)p.getCodigo() + " tiene my nota")
              sino
                escriba("Ambos estudiantes tienen notas iguales")
             finSi
            finSi
            romper
         sino
          si(id==cod)&&(p.getSig()==nulo)entonces
            n1 = (Estudiantes)p.getAnt().getNotaDef()
            n2 = (Estudiantes)p.getNotaDef()
            si(n1>n2)entonces
             escriba("el estudiante con código"+(Estudiantes)p.getAnt().getCodigo() + " tiene my nota")
             sino si (n2>n1)entonces  
               escriba("el estudiante con código "+ (Estudiantes)p.getCodigo() + " tiene my nota")
              sino
                escriba("Ambos estudiantes tienen notas iguales")
             finSi
            finSi
           romper
          sino
            n1 = (Estudiantes)p.getAnt().getNotaDef()
            n2 = (Estudiantes)p.getNotaDef()
            n3 = (Estudiantes)p.getSig().getNotaDef() 
            si(n1>n2)&&(n2>n3)entonces
             escriba("el estudiante con código"+(Estudiantes)p.getAnt().getCodigo() + " tiene my nota")
             sino si (n2>n1)&&(n1>n3)entonces  
               escriba("el estudiante con código "+ (Estudiantes)p.getCodigo() + " tiene my nota")
              sino si(n3>n1)&&(n1>n2)entonces 
               escriba("el estudiante con código"+(Estudiantes)p.getSig().getCodigo()+" tiene my nota")
         
             finSi
            finSi        
           finSi 
           romper
         finSi
       finSi
       p = p.getSig()
      finMientras
     finSi
  finMetodo

